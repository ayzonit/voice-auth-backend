services:
  - type: web
    name: voice-auth-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn App.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0